import { render } from "@testing-library/react"
import { shallow } from "enzyme"
import RenderSize from "../Cart/RenderSize"

describe("testing", ()=>{
    test("pass all", ()=>{
        let component = shallow(<RenderSize/>);
        
    })
})